var numbers = new Array();
var count = 9;
for (i = 0; i <= count; i++)
    numbers.push(i);
console.log("Array first number: " + numbers[0] + " Array last number: " + numbers[i-1])

var car = new Object(colour)
car.colour = "blank"
